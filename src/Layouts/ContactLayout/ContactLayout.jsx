import Header from "../../Components/Header/Header";

const ContactLayout = () => {
  return (
    <div>
      <header>
        <Header></Header>
      </header>
      <section> Contact</section>
    </div>
  );
};

export default ContactLayout;
